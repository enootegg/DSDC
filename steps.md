1 крок
завантажити файл

post запит на:
https://api.crowdin.com/api/v2/projects/749381/translations/exports

body:
{
"targetLanguageId": "uk",
"fileIds": [22]
}

headers:
Authorization (Bearer token, токен я вставив у .env - CROWDIN_TOKEN)

ось що повертає запит:
{
"data": {
"url": "https://crowdin-tmp.cf-downloads.crowdin.com/...",
"expireIn": "2026-03-18T19:50:29+00:00"
}
}

по цьому урлу скачається файл localization.csv

---

2 крок (конвертуємо csv у json)

у папці steps/step2 є файл parser.py
у ньому є два методи конвертації, можна конвертацію з csv у json переписати на node.js, щоб не встановлювати лишній раз пайтон
потрібно конвертувати csv скачаний у першому кроці у json цим скриптом

---

3 крок (підганяємо json версії directors cut для звичайної версії гри)

файли у steps/step3

спочатку розділяємо файл localization.json на source_show_dsdc.json та target_dsdc.json скриптом split.js
потім виконуєм скрипт merge.js щоб зібрати файл з полями звичайної версії
вставляємо весь контент з files у файлі localization_only_in_ds_not_dc.json у наш новостворений localization_ds.json також у files

(якщо є ідеї по оптимізації цього процесу, або всі скрипти виконувати одразу в одному файлі по черзі, то можеш так зробити. Головне щоб все працювало і нічого не ламалось)

---

4 крок

файли (localization_ds_not_dc.json та localization.json) можна ставити у resources/Localization і робити білд

---

Також мені треба, щоб була можливість кудись залити файли, які будуть у папці Sources, та Sources_DSDC_Only, при білді викачувати їх і вже з ними білдити інсталятор.

Може можеш щось класне для цього запропонувати і безкоштовне. Наприклад якийсь supabase чи щось схоже.

Мені потрібно буде щоб я собі міг натиснути одну кнопку і в мене збілдився інсталятор з уже актуальним файлом з crowdin і файлами Sources, які я сам в якусь папку додам. Також було б добре зробити автоматичний білд раз у 3 дня, щоб користувачі могли скачати актуальний інсталятор.

Може і це можна якось буде зробити на гітхабі чи щось таке. І одразу там релізи скачувати.

---

додатково треба ще робити при білді запит на https://api.crowdin.com/api/v2/projects/749381/files/22/languages/progress (там такий самий хедер як у 1 кроці) і діставати звідти response.data[0].data.approvalProgress (відповідь буде наприклад 40). Цей номер треба вставляти у версію білда. Наприклад 0.40 у нашому випадку.

---

файли у steps я кинув для прикладу, ти можеш переробити їх, щоб максимально оптимізувати процес і перенести в якесь інше місце

---

чекаю від тебе поради щодо файлів для Sources та автоматичному білду
